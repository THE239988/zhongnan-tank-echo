# 中南坦克 · 机甲界面更新

2026-09-12

## 运行

双击本目录 `run.bat`。本包包含原版 Windows Java 17 运行时，无需额外安装 Java、Maven 或浏览器。

本包同时包含完整源码、原版音频和原始 data 文件。测试使用独立目录，没有覆盖原始设置或战绩。原来的压缩包没有修改。

## 本次界面调整

- 主菜单采用雾白机库、白色装甲坦克与荧光色主操作，移除旧版校园插画、厚重描边和凸起按钮。
- 按钮区分主操作、次操作、图标工具，并统一悬停、按下、焦点和禁用状态。
- 出击准备分为模式、机体展示、参数三个区域，保留双人独立选车。
- 战斗 HUD 使用深色背景，分别突出生命、共振能量、金币和库存。
- 设置、排行榜、补给、暂停、强化和结算采用统一的细分隔线与间距层级。
- 机甲素材预渲染为本地 PNG，运行时由 JavaFX 显示，不新增浏览器或实时 3D 依赖。

## 改动边界

修改的原文件仅为：

- `src/main/java/tanktrouble/view/HomePage.java`
- `src/main/java/tanktrouble/view/TankTroubleApp.java`
- `src/main/java/tanktrouble/view/BattleRenderer.java`

新增 `MechaArtwork.java`、`mecha.css` 和四张机甲图片。原 `style.css` 保留，入口改用新样式。

玩法模型、AI、碰撞、地图生成、坦克数值、输入控制、音频、存档格式和依赖版本均保持原样。原有按钮事件绑定、控件 ID、画布与世界坐标换算保留。

启动流程和既有 `assets/opening.mp4` 片头接口保持原样。本次没有加入网页预览中的模拟加载进度或强制等待；后续加载动画可另行接入。

原 README 和 DEVELOPMENT 是保留的原版说明，界面更新以本文件为准。

## 验证结果

- 原版基线：101 项测试，0 失败、0 错误、0 跳过。
- 新版回归：同一套 101 项测试，0 失败、0 错误、0 跳过。
- 检查真实 JavaFX 截图，包括标准窗口与 980 × 700 小窗口。
- 回归覆盖开战、双人模式、按键响应、暂停恢复、购买补给、炮塔部署坐标、结算、强化、设置保存和片头取消/重播边界。
- 比对 67 个原有源码、测试、音频和数据文件，只有上述 3 个显示层文件发生变化。
- 63 个玩法、控制和音频 class 文件与同一 JDK 编译的原版逐字节一致。
- 13 个运行依赖 JAR 与原包逐字节一致。
- 使用最终 JAR 和本包自带 Java 运行时，另行验证 4 张机甲素材、四种坦克选择、音量设置保存、P2 独立选车以及双人模式不开放商店。

这些检查没有发现本次修改引入的回归，不代表能够保证所有硬件及所有游玩场景绝对无缺陷。

## 源码构建

修改源码后执行 `mvn package` 重新生成游戏 JAR，再运行 `run.bat`。

完整回归命令：

```powershell
mvn -B test -DfxSmoke=true -Dtanktrouble.dataDir=target/ui-test-data -Dtanktrouble.screenshots=target/ui-screenshots
```

GUI 测试会短暂打开游戏窗口，需要可用的 Windows 图形桌面。

## 3.0.2 综合整合（2026-09-15）

本版以机甲界面分支为主干，完成以下整合：

- 保留单机、无尽、本地双人、多人混战和合作 Boss。
- 并入工作目录版本的子弹晶针、拖尾、发射者配色、预瞄终点和撞墙火花。
- 并入 `坦克大战(5)` 的房间列表、聊天、房主踢人、准备和局域网自动发现。
- 本机开服后会同时广播房间，加入页可以自动发现同网房间并直接进入。
- 局域网联机和服务器联机拆成两个独立入口，不再共用一套地址状态。
- 界面排版按 `究极特效版` 重排：主页改为顶部命令栏、中央主视觉、底部三模式条与大号出击按钮；准备页恢复四列结构；战斗页恢复宽屏双栏、窄屏折叠到 HUD 的响应式布局。
- 协议版本提升为 `4`，旧版客户端不能连接 3.0.2 服务端。
- 联机聊天补充显式焦点和发送按钮处理；对局结束后保留房间，房主可以直接发起下一局。
- 全局按钮、输入框、房间列表和联机模式卡片适当放大，改善空页面和点击目标。
- 客户端连接改为独立异步发送队列和旧快照合并，慢客户端不再拖住房间广播。
- 新增 `build.ps1`、`test.ps1`、版本化服务端目录和 systemd 部署文件。

网络模式仍暂不开放商店、炮塔部署和局间强化；单机与无尽模式继续保留这些系统。
