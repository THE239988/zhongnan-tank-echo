# 3.0 开发交付记录

3.0.2 以 `TankTrouble-Mecha-UI` 为主干，保留六坦克、权威服务端、本地双人、多人混战和合作 Boss。界面层移植工作目录版本的子弹晶针、发射者配色、预瞄终点、衰减光束和撞墙火花；联机大厅加入 `坦克大战(5)` 的聊天与房主踢人能力。协议版本提升为 `4`，旧客户端不能连接 3.0.2 服务端。

## 输入核对

原压缩包中的代码实际为 `tanktrouble` 包下 7 个 Java 类：双人本地对战、随机 Prim 迷宫、圆形碰撞、60Hz 固定步长、寿命制子弹。并没有详细设计正文列出的单人 Model、A*、金币、页面状态机或 JUnit 测试。这与说明书第 10.6 节、附录 B 的差异记录一致。

本版保留原型的随机 Prim + 额外通道思路、前后驾驶和方向旋转、冷却连续开火，按设计的数据流重建可扩展结构。根据后续需求，自己发射的子弹（包括反弹和所属炮塔）不再伤害自己。原始压缩包未修改。附件里的“需组会确认”“本日只做 P0”等记录作为历史设计背景，本轮范围依据用户要求继续开发。

## 已接入范围

| 阶段 | 本版实现 |
|---|---|
| P0 对齐 | 单人 1v3、A*、8ms 仿真、AABB、扫掠弹道、金币拾取、死亡优先级、首页/准备/战斗/结果 |
| P1 | 模式选择、四种坦克、本地双人对战、无尽波次、传送门、本地战绩持久化 |
| P2 | 随机掉落、维修/护盾/急速/炮塔、暂停商店、统一局内钱包、效果到期 |
| P3 部分 | 冰面、岩浆、重力减速、坦克造型、弹道拖尾、脉冲动画、窗口自适应 |
| 新玩法 | 反弹蓄能脉冲、无尽局间三选一永久强化 |

## 明确采用的设计决定

1. 地图采用原型 12×9、80px 单元，即 960×720 世界坐标；界面按比例缩放。说明书 20×15、40px 是待定配置，此处保留原型驾驶空间。相关常量集中在 `Rules`。
2. 随机 Prim 保证所有格子连通，不采用“重试生成 + 回退地图”路径；算法有限迭代，100 个种子的全部格子可达性已测试。寻路使用 JGraphT 1.5.2 的 A*，四方向图和曼哈顿启发式。
3. 采用设计正文的固定 8ms、AABB 和最多 6 次反弹；增加 10 秒子弹寿命作为存活上限。炮口禁止跨墙出生，支持无遮挡方向贴墙开火。
4. 四种坦克的名字、HP、速度和装填是本轮提出的可调整数值；均衡型与正文 10 HP 基线一致。敌军从 1 HP 开始。
5. “双人模式”按已交付原型理解为本地对战。没有将其擅自改为合作；单人和无尽共用统一局内钱包，双人对战不开放经济系统。合作共享钱包属于后续扩展。
6. 单人击破最后敌人立即结算，不自动吸取金币。无尽单独增加 5 秒回收窗口，之后进入强化。计分只来自拾取金币，不因击杀或消费重复变动。
7. 传送门仅传送坦克，敌人也可使用。出口必须通过同一套坦克碰撞校验。脉冲清弹范围可跨墙，脉冲伤害受墙体遮挡及护盾保护。
8. 冰面惯性与重力减速当前作用于玩家驾驶；岩浆对所有坦克有效。AI 保持沿网格通道的稳定驾驶，尚未实现惯性寻路。
9. 数据类型集中在 `GameData` 的 records / enums，可变仿真实体集中在 `Entities`；没有为每个简单值类型建立单独文件。效果和计分当前由 `TankGameModel` 统一推进，尚未独立拆出 `ItemSystem`、`TerrainSystem`、`ScoreSystem`。
10. 坦克、地形和战场使用 JavaFX Canvas 原创程序绘制，没有下载不明来源图片。新增 A* 库及依赖声明见 `THIRD_PARTY.md`。

## 验证

执行 `mvn test -DfxSmoke=true -Dtanktrouble.dataDir=target/test-data` 可运行纯 Model 回归和独立 JavaFX 界面测试。普通 `mvn test` 不启动窗口。

- 地图：100 个种子 × 108 个格子的安全出生和可达性、种子重现、A* 长度与 BFS 对照。
- 碰撞：高速薄墙、离墙不重复反弹、墙角双轴反射、360 度炮口不自交、炮口穿墙阻止、墙/坦克最早碰撞顺序、第 6 次销毁、反弹免伤自身、运动进入敌方子弹的初始重叠。
- 规则：固定步长守卫、只读快照、暂停清键、金币单次结算、购买守卫与扣费、效果到期、传送冷却与堵塞出口、同归于尽优先级、死亡同 Tick 掉落、无尽升级、脉冲、护盾与岩浆、炮塔数量与寿命、双人独立输入。
- 持久化：重建 Repository 后仍可读，损坏单条记录不影响其余条目，活动对局不提前写榜。
- 仿真：12 个种子的最多 5000 Tick 自动驾驶/开火，不出现非法坐标；这不是长时间玩家平衡测试。
- 界面：按钮和键盘事件驱动真实 JavaFX 场景，覆盖全部主流程与补给/强化；窗口失焦暂停；常规尺寸和 980×700 小窗口截图；画面像素非空检查。

界面测试为可控夹具，会设置金币或敌人状态以验证分支，不代表人工从开局完整通关。实际 Windows 窗口已启动并观察。尚未进行多人长时间试玩、不同显卡/DPI 的全面兼容验证或音效检查。

## 页面与炮塔更新

`TankTroubleApp.Page` 管理首页、准备、设置、排行榜和战斗页面；Model 继续管理战斗状态，`DEPLOYING` 状态冻结仿真。排行榜记住来源页面，菜单页面不接收战斗快捷键。`GameSettings` 将地形和全屏选项写入 `data/settings.properties`。

`HomePage` 独立承载主菜单和开场视频/动画。启动时会尝试播放项目根目录 `assets/opening.mp4`，视频结束后才显示主菜单；视频缺失或格式不受支持时直接显示菜单，播放期间按 Esc 可跳过。JavaFX Media 依赖已加入 Maven 配置，便携包构建会一并复制 `assets` 目录。点击“开始游戏”仍可通过 `setOpeningAnimation(Animation)` / `playOpening` 接入额外的按钮过场。

炮塔库存通过 `TankView.turretStock()` 暴露。Model 校验购买容量和部署位置，View 将缩放后的 Canvas 点击坐标还原为世界坐标。炮塔寿命从部署后恢复仿真开始递减，无效放置和取消不扣库存。自有子弹在碰撞候选阶段忽略所属坦克，穿过自身后仍可命中其他坦克。

## 敌军协同与躲避更新

参考输入为组员提供的早期原型，重点阅读其 `EnemyAi`、`Config`、`Tank` 与 `GameState`。该原型仍保留敌军互伤，本项目根据本轮要求增加敌军阵营免伤；借鉴其迎面弹检测、垂直探路、固定方向短时躲避、射后侧移与倒车脱困思路，适配现有 JGraphT A*、AABB 和 8ms 仿真。

没有把参考工程的 104px 格宽与 180px/s 敌军速度直接移植。当前地图维持 80px 格宽，敌军速度维持 90~145px/s；探路精度为 1px，躲避持续 480ms，射后转移 400ms，受阻 600ms 后倒车 350ms。参数集中在 `Rules`，每辆坦克单独持有计时状态，传送后清空。

`CombatSystem.canHit` 同时用于子弹碰撞和 AI 威胁筛选。敌军子弹会忽略同阵营坦克，玩家子弹、玩家炮塔及双人互击仍有效。Model 将现有子弹传给 AI，消费其开火请求；只有实际发射成功才启动射后侧移。开火检测覆盖炮口、真实射线、玩家命中范围、墙体与前方友军。距离过近、炮弹初始与目标重叠时仍允许有效射击。

新增 `AiTest`、`EnemyBehaviorTest` 和敌军阵营碰撞回归，覆盖窄走廊中的实际避弹、友方/远离/被墙阻挡/即将失效的子弹、友军占位、方向保持、射后恢复追击、近距离开火、受阻倒车和传送重置。八张固定地图各模拟 60 秒，在禁用特殊地形且玩家不开火、保持护盾的夹具下，敌军无互相击毁、无穿墙或坦克重叠，仍能移动并寻找开火机会。该结果不是所有地图下的躲避率或玩家难度保证。

## 校园卡通主题

根据用户提供的校园插画调整全部 JavaFX 页面：草绿操场、红砖建筑、深色描边、奶黄立体按钮。`CampusArtwork` 用固定画板和等距投影绘制原创校园背景，窗口变化时保持比例；`HomePage` 负责描边标题和主菜单，继续保留开场动画接入及取消机制。设置与排行榜复用校园背景，准备页提供坦克展示和属性条，战斗、补给、强化与结果页使用统一的 `style.css`。

战场依旧采用清晰的俯视图，草地纹理、砖墙及坦克仅改变绘制方式，未改变碰撞矩形、子弹判定、敌军算法或炮塔部署坐标。此版本没有把参考图直接放进游戏，也没有接入新的网络资源或图片生成 API。现有界面测试增加准备页及排行榜截图，覆盖常规窗口与 980×700 的展示。

## 地形定时刷新

冰面、岩浆、重力区保持各一块，开局随机生成，每累计 10,000ms 战斗时间同步换位。`Rules.TERRAIN_REFRESH_MS` 控制间隔，`TankGameModel` 在固定步长内累加波次内计时，暂停、商店、部署、升级选择及结果页不推进。新局/新波次清零，关闭特殊地形时不刷新。原地形效果和墙体布局保持不变。

刷新候选使用完整矩形避开墙、活坦克、有效炮塔、传送门、补给和上一轮地形；随机抽取三个不同格子，使用战局种子支持重现。极端情况下不足三个空位则保留旧布局并在下个周期重试。`BattleRenderer` 从快照逐帧绘制，不需额外 UI 计时器。

新增 `TerrainRefreshTest` 的 9 项测试覆盖 9,992/10,000/20,000ms 边界、冻结状态、新局/波次重置、关闭地形、跨格边界实体避让、旧岩浆伤害终止、快照不可变、20 个种子多轮复现及空间不足恢复。独立编译 Model 并通过 JUnit 5.10.3 运行全部 80 项 Model 测试，均通过；再直接加载更新后的 JAR 运行相同测试及便携包 JavaFX 界面回归，也均通过。原包保存在 `target/terrain-check/before`。此次使用本机已有依赖验证并更新运行包的 Model 类；上一轮片头新增的 JavaFX Media 依赖仍未下载，不宣称完成整体 Maven 重建。

## 音效与音乐

上一节记录的构建遗留问题在本轮定位并修复。`pom.xml` 早已声明 `javafx-media`，依赖也已存在于本机仓库，但它从未被复制进 `target/lib`，因此 `run.bat` 按现状无法启动；实测确认 `target/tanktrouble-javafx-2.0.0.jar` 内的 `HomePage.class` 是 11:19 的旧产物，比含片头视频的源码更早，分发包实际运行的是没有片头的版本。本轮执行了一次真正生效的 `mvn -B package`，`javafx-media` 的两个 jar 已进入 `target/lib`，`run.bat` 的守卫条件也从「存在 opening.mp4 且缺少媒体库」改为「缺少媒体库即重建」——原条件在既没有视频、也没有媒体库时不会触发重建。

**事件通道**。音效不能由 View 比对帧间差异推断：同一 tick 内生成又消亡的子弹、恰好等量的一换一都会漏判。改由 `TankGameModel` 在触发点追加 `GameEvent(type, player, x, y)`，纯数据、不含 JavaFX 类型，Model 的分层约束没有被破坏。`drainEvents()` 取走并清空，保证每个事件恰好投递一次；非 `tick` 路径（`startBattle`、`chooseUpgrade`）产生的事件也会缓冲到下次取走，不会被丢弃。缓冲上限 256 条，超出即丢弃，避免异常情况下的无界增长。

`damage()` 增加了 `causedByPlayer` 参数，用于区分射击命中与岩浆伤害——否则岩浆会把「你被击中」报成玩家自己造成的。护盾吸收原本不产生任何事件，玩家被挡住时没有任何反馈，本轮补了独立的 `SHIELD_BLOCK`（原先只能靠 `shields` 计数推断）。

**播放**。音效用 `AudioClip` 而非 `MediaPlayer`：前者预解码进内存、无启动延迟、天然支持重叠播放，密集交火时 `MediaPlayer` 会有可感知的延迟。音乐才用 `MediaPlayer`（流式、原生循环）。同一音效有 45ms 重复抑制，因为一枚反弹中的子弹会在单个 tick 内触发多次反弹事件。

**反射降级**。`audio/Clips` 是唯一接触 `javafx.scene.media` 的类，且全部通过 `Class.forName` 反射调用；媒体模块缺失时 `AudioManager` 整体空操作。这不是洁癖：本轮之前，正是这个依赖的缺失让游戏在首帧前就抛 `NoClassDefFoundError`。降级的代价是调用点略啰嗦，但换来了「拿到残缺构建也能跑，只是没声音」。

**素材**。16 个音频全部由 `audio/ToneSynth`、`SoundEffects`、`Music` 程序合成，无第三方素材。设计上玩家音效比敌军音效更亮更响，避免同一 tick 内多声齐发时互相淹没；每段音效都很短（开火约 420ms 一次，超过约 250ms 就会糊到下一发）。BGM 为 8 小节循环，22050Hz 单声道。

**验证与边界**。新增 `ModelEventTest` 13 项（事件触发时机、drain 后不重放、不可变、非 tick 路径投递、玩家与敌军可区分、岩浆不归属玩家、护盾独立上报），`GameSettingsTest` 扩到 6 项（音量持久化、越界钳制、非法值回退、旧设置文件无音量字段时仍可加载且不丢失其他偏好）。全量 101 项通过，界面回归（`-DfxSmoke=true`）通过。

音频链路用独立探针在真实 JVM 中验证：正常 classpath 下 12 个音效全部加载、BGM 切换与播放成功；移除 `javafx-media` 后 `enabled=false`、加载 0 个、全部 API 调用仍正常返回，确认降级路径成立。

**未验证部分**：音色是否好听、BGM 是否喧宾夺主、各音效之间的响度平衡，都无法由测试或编译判定，需要人工试听。当前参数只保证「文件合法、无削波、事件不误触发、音量可调」。`assets/audio/` 中的文件可同名直接替换。

## 后续边界

当前没有音效/背景音乐、联网、账号、双人合作、可破坏墙、键位重绑定和完整无障碍操控。P3 的数值平衡需要真实试玩反馈。说明书中的状态图和 draw.io 源图没有附在压缩包中，本版未伪称已同步修改这些图。
